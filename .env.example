# Supabase Configuration
# Set to 'true' to use Supabase database (requires Supabase setup)
# Leave unset or 'false' to use mock data (default)
USE_SUPABASE=false

# OpenAI API Key (Optional)
# Used for text embeddings in vector search functionality
# If not set, embeddings will return zero vectors (graceful fallback)
OPENAI_API_KEY=
